# Relationships

## Call Graph

```mermaid
graph LR
{% for rel in relations[:30] %}
{% if rel.type.value == "calls" %}
    {{ rel.source_id.split(":")[-1] }} --> {{ rel.target_id.split(":")[-1] }}
{% endif %}
{% endfor %}
```

## All Relations

| Source | Type | Target |
|--------|------|--------|
{% for rel in relations[:50] %}
| {{ rel.source_id.split(":")[-1] }} | {{ rel.type.value }} | {{ rel.target_id.split(":")[-1] }} |
{% endfor %}

---

[← Back to Overview](./index.md)
